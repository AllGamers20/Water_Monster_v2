import 'package:flutter/material.dart';
import '../Widgets/navbar.dart';

class Home extends StatelessWidget {
  const Home({super.key});

  @override
  Widget build(BuildContext context) {
    return const nav_bar();
  }
}
