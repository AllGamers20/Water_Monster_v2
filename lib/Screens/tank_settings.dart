import 'package:flutter/material.dart';

import '../Widgets/navbar_task_setting.dart';

class T_settings extends StatelessWidget {
  const T_settings({super.key});

  @override
  Widget build(BuildContext context) {
    return const navbar_tank_settings();
  }
}
